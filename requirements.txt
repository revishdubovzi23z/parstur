# Locked production dependencies. Edit requirements.in (the
# top-level constraints file) and run `pip-compile requirements.in`
# to regenerate this file. See requirements.in for the workflow.
#
# Until the first `pip-compile` is run, this file is hand-curated to
# match what's installed in production today (and what requirements.in
# expresses as exact-pin constraints). Both files MUST stay in sync.

requests==2.33.1
beautifulsoup4==4.14.3
parse-torrent-name==1.1.1
python-dotenv==1.2.2
fastapi==0.136.1
uvicorn==0.46.0
pydantic==2.13.3
pydantic-settings>=2.4
HdRezkaApi==11.2.3
requests-cache==1.3.1
websockets>=12.0
aiohttp>=3.9.0
