# 5.7: dev-only dependency manifest (CI lint / typecheck / test
# tooling). NOT installed in production. Build the locked file with:
#
#     pip-compile requirements-dev.in
#
# and commit requirements-dev.txt alongside this file.

-c requirements.txt

ruff==0.6.9          # in lock-step with .pre-commit-config.yaml
mypy>=1.11
pytest>=8.0
pytest-asyncio>=0.23
pre-commit>=3.5
pip-tools>=7.4       # provides `pip-compile`
