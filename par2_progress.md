# Прогресс по `par2_code_review.md`

Это рабочий файл тестовой ветки `devin/test`. В `main` ничего из этого пока не вливается — ветка живёт отдельно, по мере прогресса я отмечаю выполненное и складываю исправления сюда.

Полный исходный документ с описанием каждого пункта: [`par2_code_review.md`](./par2_code_review.md) (когда он окажется в репозитории) либо в комментарии PR.

## Легенда
- `[x]` — сделано в этой ветке.
- `[~]` — в работе.
- `[ ]` — ещё не трогал.
- `(?)` — нужна доп. информация / решение от пользователя.

---

## 1. Критические баги

- [x] **1.1** `fix_posters.py` уничтожал корректные одинаковые KP/IMDb оценки. Удалён разрушительный `UPDATE items SET kp_rating = 0, imdb_rating = 0, is_metadata_fixed = 0 ...` и осиротевшая `cats_str`. Файл: `fix_posters.py`.
- [x] **1.2** `start_rezka_collections` — заменён антипаттерн `if check_any_running():` на прямой вызов `check_any_running()`. Файл: `main.py`.
- [x] **1.3** `start_cleanup` — добавлен `check_any_running()` и `return {"status": "started"}`. Cleanup больше не может стартовать параллельно с другим job-ом, и фронт получает корректный ответ. Файл: `main.py`.
- [x] **1.4** Path traversal в `POST /api/stop/{key}`. Введён whitelist `VALID_STATUS_KEYS` (производная от `process_status`), `_is_valid_status_key()`. Применён в `stop_process` (входная валидация + active_pipeline_key) и `_read_progress`. Файлы: `main.py`.
- [x] **1.5** WebSocket `/ws` обходил `auth_middleware`. Сервер: `/ws` принимает `?token=...`, при включённом auth закрывает сокет с кодом `4401` без валидного токена. Фронт: `connectWebSocket()` дописывает токен; `mounted` ждёт `checkAuth()`; `doLogin` зовёт `connectWebSocket()`; в 401-обработчике WS закрывается. Файлы: `main.py`, `index.html`.
- [x] **1.6** `startSync` и `startFix` переведены на `this.apiFetch`. При включённом auth теперь корректно получают `Authorization`, на 401 отрабатывает модалка логина, а лишний `alert('Ошибка запуска')` больше не дублирует сообщение. Файл: `index.html`.
- [x] **1.7** `db.find_existing_item` теперь сверяет `category_id` для веток `kp_id` / `imdb_id` / `rezka_url`, когда категория передана. Один и тот же external id в разных категориях больше не приводит к молчаливой склейке карточек. Файл: `db.py`.

## 2. Безопасность

- [x] **2.1** Аутентификация:
  - [x] `secrets.compare_digest` и для логина, и для пароля.
  - [x] Поддержка `AUTH_PASS_HASH` (`pbkdf2_sha256$<iter>$<salt>$<hash>`, 600 000 итераций), `AUTH_PASS` оставлен как fallback ради обратной совместимости. Однострочник для генерации хеша — в `main.py` рядом с объявлением переменной и в `.env.example`.
  - [x] `_session_tokens` теперь реально хранит Unix-epoch expiry (раньше булево по `dict[str, float]`). Sliding TTL — 7 дней, обновляется на каждом успешном `_check_token`. WS использует тот же helper.
  - [x] `POST /api/logout`: инвалидирует переданный bearer и всегда возвращает 200; whitelisted в auth-middleware. UI-кнопку логаута решено добавить отдельно, эндпоинт уже доступен.
  - [x] `/api/debug/queue` отдаёт 404 при выключенной авторизации (раньше любой мог получить `process_status`/состояние очереди). При включённой — middleware и так требует Bearer.
  - Файл: `main.py`.
- [x] **2.2** Параметризован SQL: введён хелпер `db._placeholders(values)`, перепаханы `db.get_feed`, `db.get_categories_with_counts`, `db.get_stats`, `db.export_items`, `db.get_db_stats`, `db.get_items_needing_metadata` и `reprocess_database.reprocess_all`. `category_id IN (...)`, `hide_rated NOT IN (...)`, `JOIN ... AND ci.collection_id = ?`, `LIMIT ?` теперь идут через `?`-параметры. `check_col` в `get_items_needing_metadata` ограничен whitelist-ом `_METADATA_CHECK_COLS`. Удалён неиспользуемый kwarg `extra_where`. Файлы: `db.py`, `reprocess_database.py`.
- [x] **2.3** Добавлены `.env.example` (шаблон со всеми переменными окружения, которые читает приложение, плюс однострочник генерации `AUTH_PASS_HASH`) и `SECURITY.md` (`.env`-гигиена, рекомендация заводить **отдельный** сервис-аккаунт на HDRezka, обращение с БД/логами, как сообщать о проблемах). Файлы: `.env.example`, `SECURITY.md`.
- [x] **2.4** Добавлен middleware `security_headers_middleware`, который вешает `Content-Security-Policy`, `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy: same-origin`, `Permissions-Policy` (geolocation/camera/microphone/payment/usb/interest-cohort = `()`). CSP учитывает Tailwind Play / Vue 3 / SortableJS из CDN. HSTS не добавлен умышленно — приложение часто работает по HTTP локально. Файл: `main.py`.
- [x] **2.5** Хост Rutor читается из `RUTOR_MIRROR` (по умолчанию `https://rutor.info` вместо `http://`), чтобы куки и трафик не утекали в plaintext. Заменены все три hard-coded `http://rutor.info` (`rutor_parser.py`, `reprocess_database.py`, `single_item_update.py`). Документировано в `.env.example`. Файлы: `rutor_parser.py`, `reprocess_database.py`, `single_item_update.py`, `.env.example`.

## 3. Логика и поведенческие баги

- [x] **3.1** Введён единый источник правды `PROCESS_KEYS: tuple[str, ...]` в `main.py`. И `running_processes`, и `process_status` строятся из него dict-comprehension-ом — расхождение между ключами (`poiskkino`, `rezka_collections` отсутствовали в `running_processes`) больше невозможно. `VALID_STATUS_KEYS` стал производной от `PROCESS_KEYS`.
- [x] **3.2** `sync_job.py` — критерий ранней остановки переписан: считаем подряд идущие пустые страницы, выходим после двух подряд (`empty_pages_in_a_row >= 2`). Хрупкий `len(new_ones) < len(raw) - SYNC_STOP_THRESHOLD` убран. Заодно undated-релизы (None из `parse_rutor_date`) пропускаются как «возможно свежие», а не как «старее курсора».
- [x] **3.3** `parse_rutor_date` теперь возвращает `None` при ошибке парсинга. На стороне вставки релиза (`sync_job.py`) `None` логируется и заменяется на `datetime.now()` явно — это уже не молчаливое поведение хелпера.
- [x] **3.4** `cleanup_duplicates.py` — пороги вынесены в константы `NAME_SIMILARITY_STRICT = 0.85` и `NAME_SIMILARITY_FUZZY = 0.6`. Логика `do_merge` переписана: для KP/IMDb/Rezka групп при разнице лет > 1 требуется 0.85, при равных/неизвестных годах хватает 0.6; для name-only групп требуется 0.85 либо (`same_year` + 0.6).
- [x] **3.5** `from difflib import SequenceMatcher` поднят на уровень модуля в `cleanup_duplicates.py`.
- [x] **3.6** `db.get_feed` теперь вытягивает релизы одним `SELECT * FROM releases WHERE item_id IN (...) ORDER BY item_id, date_added DESC` и группирует их в `releases_by_item: dict[int, list]` в Python. N+1 убран.
- [x] **3.7** Добавлен `db._materialize_id_list()` хелпер: при `>= 256` id-шниках создаёт `TEMP TABLE`, и `NOT IN` пере­писывается в `NOT IN (SELECT id FROM temp.<name>)`. Применён в `get_feed` (hide_rated) и `get_categories_with_counts`. Маленькие списки идут прежним `?`-путём. TEMP-таблица сбрасывается перед `return`.
- [x] **3.8** `db.get_categories_with_counts` — 14 сканов сократились до 3. Per-category counts: один `LEFT JOIN items ... GROUP BY c.id` (фильтры hide_rated/hide_collected — в `ON`, не в `WHERE`, чтобы пустые категории не отваливались). 6 video-scoped корзин (Все видео / БЕЗ ПОСТЕРОВ / БЕЗ ОЦЕНОК / БЕЗ КП ID / БЕЗ IMDb ID / БЕЗ ID ВООБЩЕ) — один SELECT с `SUM(CASE WHEN ...)`. count_any + count_ignored — ещё один.
- [x] **3.9** FTS5 tokenize заменён на валидный `'unicode61 remove_diacritics 2'`. Старая строка `'unicode61 categories UnicodeL* L*'` молча падала и FTS поднимался без диакритик-фолдинга. x↔х остаётся ответственностью `title_norm` (через `app_core.normalize_title`), которая тоже индексируется.
- [x] **3.10** Удалён `py_lower` UDF (нигде не использовался) и неиспользуемый `import unicodedata` в `db.py`.
- [x] **3.11** `setup_tee_logger` теперь tee-ит и stdout, и stderr. `TeeWriter` по-прежнему пишет в живой консоль (`sys.__stdout__` / `sys.__stderr__`) и в файл лога — tracebacks больше не теряются.
- [x] **3.12** `script_utils.save_checkpoint` стал атомарным: `tempfile.mkstemp` рядом с целевым файлом → `json.dump` → `f.flush()` + `os.fsync` → `os.replace`. На частичной записи `os.unlink(tmp)` чистит огрызок. Прежний resume-point больше не зануляется при kill/oom.
- [x] **3.13** `reprocess_database.py` — глубокий поиск KP/IMDb по торрент-страницам Rutor ограничен первыми 3 релизами (`rels[:3]`). Если в первых трёх нет линка на КП/IMDb, дальше его не будет тем более; per-item бюджет стал ограниченным.
- [x] **3.14** `rezka_sync._async_search` / `_async_load_page` теперь складывают `(kind, key, error_class)` в `_BATCH_ERRORS` и в конце каждой phase 1a/1b/1c/N печатается агрегат `'aggregated transport errors: total=12 search HTTP 503=10, ClientConnectorError=2'`. Молчаливые нули больше не путают оператора.
- [x] **3.15** Добавлен `rezka_sync._login_cookies()`: при наличии `REZKA_EMAIL` / `REZKA_PASSWORD` логинится через `HdRezkaSession`, переносит cookiejar в dict и передаёт его в `aiohttp.ClientSession(cookies=...)`. При неудаче / отсутствии кредов — fallback на анонимный `{'hdmbbs': '1'}` (с однострочным warning-ом). Те же env-переменные уже использует `rezka_collections_sync.py`.
- [x] **3.16** `base_client._request` теперь спит `request_delay` только если `response.from_cache` — `False`. Кеш-хиты обрабатываются мгновенно.
- [x] **3.17** `base_client._request` различает `404` (новый флаг `self.not_found = True`) и временные сбои (`5xx` / `requests.Timeout` / `requests.ConnectionError` → `self.network_error = True`). Скрипты могут аккуратно ставить `checked_* = 1` только когда `not_found` — транзиентный 502 больше не делает строку un-retryable до ручной правки.
- [x] **3.18** `tmdb_client.get_external_ids` логирует и не-2xx (`HTTP <code>`), и исключения (`<class>: <msg>`). Различие «нет маппинга» / «нас прибили» снова видно в логе.
- [x] **3.19** Удалена в рамках рефакторинга 3.4: для KP/IMDb/Rezka групп теперь применяется один и тот же набор порогов (`NAME_SIMILARITY_FUZZY` при близких годах, `NAME_SIMILARITY_STRICT` при разнице > 1 года), без отдельной слабой проверки 0.4.
- [x] **3.20** Удалён мертвый `class RutorParser` из `reprocess_database.py` (одно неиспользуемое поле `self.mirror`) и осиротевшая `rutor = RutorParser()`.
- [x] **3.21** Введён `_evaluate_candidate(page_kp_id, page_imdb_id, page_year, scored_item, year, kp_id, imdb_id, *, print_id_check_line)` — общий verdict-helper. `_verify_candidate` (по `HdRezkaApi`) и `_verify_candidate_soup` (по `BeautifulSoup`) теперь только готовят входные данные и делегируют, ~70 строк копипаста ушло.

## 4. Производительность

- [x] **4.1** Добавлен `Database.wal_checkpoint(mode='TRUNCATE')` хелпер. В `main.py` startup-обработчик гонит один `TRUNCATE`-чекпоинт (реапит WAL предыдущего запуска), а фоновая asyncio-задача `_wal_checkpoint_loop` вызывает его каждые 30 минут. Shutdown отменяет таску до общего завершения. Логирует `busy=1` без ошибки (длинный читатель блокирует, следующая итерация переуспеет). На локальном тесте 9.6 МБ WAL после 20k вставок схлопывается в 0 байт. Файлы: `db.py`, `main.py`.
- [x] **4.2** Введены `NAME_GROUP_WARN = 50` и `NAME_GROUP_MAX = 200`. Бандлы по `(clean_t(title), category_id)` размером > 200 теперь пропускаются с одной строкой `[CLEANUP] skipping name group ...: size=N > 200 (sample: ...)` — это всегда регрессия `clean_t()` или сломанный `category_id`, не реальные дубли. Бандлы >= 50 идут как обычно, но с heads-up-логом, чтобы аномалия не уходила в тишину. Файл: `cleanup_duplicates.py`.
- [x] **4.3** В `_ensure_indexes()` (`db.py`) добавлены: `idx_items_cat_ignored ON items(category_id, is_ignored)`, partial `idx_items_kp_id` / `idx_items_imdb_id` / `idx_items_rezka_url` (только для не-NULL и не пустых строк, чтобы индекс не раздувался), `idx_releases_item_date ON releases(item_id, date_added DESC)`, partial `idx_releases_rutor_id`, `idx_collection_items_item ON collection_items(item_id)` (PK был `(collection_id, item_id)` — обратное направление лежало без индекса). Файл: `db.py`.
- [x] **4.4** `rezka_sync.REZKA_CONCURRENCY` теперь идёт по приоритету: `--concurrency N` CLI-флаг → `REZKA_CONCURRENCY` env → `config.json: rezka.concurrency` (по умолчанию 6) → fallback 6. Default поднят с 3 до 6. CLI-флаг отвергает `< 1`, нечисловой env логирует warning и не зависает. Документировано в `.env.example`. Файлы: `rezka_sync.py`, `config.json`, `.env.example`.
- [x] **4.5** Закрыт в рамках 3.16 — `base_client._request` теперь спит `request_delay` только когда `response.from_cache` — `False`.
- [x] **4.6** `api_cache.get_cached_session()` пробрасывает в `CachedSession` `fast_save=True` и `wal=True` для sqlite-бэкенда (оба флага поддерживаются `requests-cache` 1.3.x). Конфиг `cache.fast_save` / `cache.wal` в `config.json` позволяет выключить, если оператор хочет дефолтные синхронные дюрабилити. Не-sqlite бэкенды (memcached/redis) флаги игнорируют. Файл: `api_cache.py`.
- [ ] **4.7** *(deferred — большой архитектурный ход)* Tailwind CDN → Vite/PostCSS-сборка. Решение в этой ветке делать не стал: правка слишком большая (генерация `dist/`, обновление CSP, smoke-тест Tailwind-классов на всех экранах) и должна идти параллельно с 4.8/5.9. Оформить отдельной веткой `devin/frontend-bundling`.
- [ ] **4.8** *(deferred — большой архитектурный ход)* Vue из CDN → Vite + Vue 3 + TypeScript. Парный с 4.7 (Tailwind) и 5.9 (split `index.html`). В одной ветке без переписывания фронта не вытащить — требует отдельного review-цикла.
- [ ] **4.9** *(deferred — большой архитектурный ход)* Разбить `db.py` (1530 строк) и `main.py` (1690 строк) на модули (`db/{items,feed,collections,user_ratings}.py`, `api/{auth,feed,process_control}.py`). В одной ветке такой split конфликтует со всеми остальными правками — оформить отдельной серией PR с предварительным `mv`-only коммитом.

## 5. Качество кода и архитектура

- [x] **5.1** Добавлен `tests/` с `conftest.py` (fixture `tmp_db`) и шесть юнит-файлов под `pytest` + `pytest-asyncio`: `test_app_core.py` (normalize_title, clean_title_for_search, x→х fold), `test_parse_rutor_date.py` (все российские месяцы, "Сегодня"/"Вчера", двузначные годы, fallback на январь, контракт `None` для нераспознаваемого), `test_db_helpers.py` (`_placeholders`, `_materialize_id_list`, regression на SQL-injection и empty-set NULL sentinel), `test_find_existing_item.py` (контракт 1.7 — category-aware lookup для kp_id/imdb_id/rezka_url, ±1 год для title_norm fallback), `test_clean_t.py` (`cleanup_duplicates.clean_t`: годы, alt-title split, soft-bug «1080 съедается year-regex»), `test_save_checkpoint.py` (атомарность 3.12: round-trip, перезапись, `os.replace`-failure → исходный файл цел, UTF-8). Итого 80 тестов, ~0.5 сек. Файлы: `tests/`.
- [x] **5.2** Добавлены `pyproject.toml` с конфигом `ruff` (line-length 100, target py310, выбран профиль `E`/`W`/`F`/`I`/`B`/`UP`/`SIM`/`RUF`, отключён шум вокруг русского unicode `RUF001-003`, стилистики `SIM102/103/105/117`, легаси `UP006/007`), `ruff format` (двойные кавычки, табы), `mypy` (`follow_imports=silent`, `ignore_missing_imports`, override на `script_utils` со строгими `disallow_untyped_defs` / `warn_return_any`), `pytest` (`asyncio_mode=auto`). Добавлен `.pre-commit-config.yaml` с `pre-commit-hooks` (trailing-whitespace, end-of-file-fixer, check-yaml/toml, large-files cap 2 MB) и `ruff-pre-commit` (`ruff` + `ruff-format`). Поправлен реальный `F821` в `main.py` (хойстнут импорт `JSONResponse`). `ruff format .` проехал по всему репо. Файлы: `pyproject.toml`, `.pre-commit-config.yaml`.
- [x] **5.3** Добавлен `.github/workflows/ci.yml`. Три параллельных джоба: **lint** (`ruff check . && ruff format --check .`, версия ruff приколочена к `.pre-commit-config.yaml` чтобы локально и в CI не расходились), **tests** (`pip install -r requirements.txt`, `pytest -v` по `tests/`), **types** (`mypy script_utils.py` по строгому allow-list из `pyproject.toml`, `continue-on-error: true` пока allow-list не разрастётся). Добавлены аннотации в `script_utils.py` чтобы строгий блок прошёл чисто. Файлы: `.github/workflows/ci.yml`, `pyproject.toml`, `script_utils.py`.
- [ ] **5.4** *(deferred — большая инвазивная правка)* `print` → `logging`. Каждый сценарный файл (`sync_job`, `fix_posters`, `rezka_sync`, `reprocess_database`, `cleanup_duplicates`, `single_item_update`, `user_sync`) делает десятки `print(...)` для прогресс-логов, читаемых `logger.setup_tee_logger`. Полный переход на `logging` требует а) выровнять формат (русский/английский/emoji), б) перевести `setup_tee_logger` на `logging.Handler`, в) перепробовать каждый сценарий end-to-end. Семантической пользы для one-shot-скриптов мало; лучше отдельной веткой `devin/logging-migration` после того как 5.10 (стиль) согласован.
- [x] **5.5** Добавлен `settings.py` с единым классом `Settings`, собранным из пяти доменных миксинов через `pydantic-settings`: `_AuthSettings` (`AUTH_USER`/`AUTH_PASS`/`AUTH_PASS_HASH`), `_RezkaSettings` (`REZKA_EMAIL`/`REZKA_PASSWORD`/`REZKA_CONCURRENCY` с `ge=1`), `_SyncSettings` (`SYNC_MIN_YEAR`/`SYNC_MAX_YEAR`/`STATUS_KEY`), `_ApiKeysSettings` (`KINOPOISK_API_KEY`/`TMDB_API_KEY`/`POISKKINO_API_KEY`), `_StorageSettings` (`APP_DATA_DIR`/`DB_PATH`/`API_CACHE_PATH`/`RUTOR_MIRROR`). `case_sensitive=False`, `extra=ignore`, `env_file=.env`. Singleton `settings`, `reload_settings()` для тестов. Добавлено 5 тестов (`tests/test_settings.py`). В `requirements.in/.txt` добавлен `pydantic-settings>=2.4`. Существующие `os.getenv(...)` сайты не тронуты — миграция одного файла за раз отдельными коммитами по мере обхода. Файлы: `settings.py`, `tests/test_settings.py`, `requirements.in`, `requirements.txt`.
- [x] **5.6** Добавлен лёгкий runner миграций под `PRAGMA user_version`. Файлы `migrations/NNNN_<slug>.sql` сортируются по 4-значному префиксу; runner (`Database._apply_migrations`) пробегает по списку, применяет всё с префиксом > текущего `user_version` через `executescript`, обёрнутый в `BEGIN`/`COMMIT` + `PRAGMA user_version = N`. Вызывается из `init_schema` (свежие БД) и `check_and_migrate_schema` (legacy). Не-`NNNN_*.sql` файлы (например, `README.md`) игнорируются. `0001_baseline.sql` — no-op маркер чтобы существующие установки получили `user_version = 1` и будущие миграции имели floor. README с workflow / rationale (почему не Alembic). Добавлено 5 тестов (`tests/test_migrations.py`). Файлы: `db.py`, `migrations/README.md`, `migrations/0001_baseline.sql`, `tests/test_migrations.py`.
- [x] **5.7** Добавлены `requirements.in` (top-level production-зависимости с exact-pin констрейнами, чтобы первый прогон `pip-compile` не дёрнул незапланированных апгрейдов) и `requirements-dev.in` (CI/линт/тест: `ruff==0.6.9` в lock-step с pre-commit, `mypy`, `pytest`, `pytest-asyncio`, `pre-commit`, `pip-tools`, констрейн `-c requirements.txt` чтобы не было solver-конфликта). В шапке `requirements.txt` явный комментарий «edit `requirements.in` and re-run `pip-compile`». Файлы: `requirements.in`, `requirements-dev.in`, `requirements.txt`.
- [x] **5.8** Добавлены `Dockerfile` (multi-stage `python:3.12-slim`, build-deps только в builder, runtime копирует только `~/.local`, не-root user `par2:par2` UID 1000, `EXPOSE 8000`, healthcheck на `/api/check_auth`), `docker-compose.yml` (`build: .`, читает `.env`, переопределяет `APP_DATA_DIR`/`DB_PATH`/`API_CACHE_PATH` чтобы данные жили на host volume `./data`, `restart: unless-stopped`, healthcheck зеркалит Dockerfile), `.dockerignore` (исключает `.git`, `.env`, `tests/`, локальные `*.db`/`*.sqlite`, прогресс/чекпойнт/стоп-флаги, dev-only конфиги). Команды для запуска — `docker compose up --build`. Файлы: `Dockerfile`, `docker-compose.yml`, `.dockerignore`.
- [ ] **5.9** *(deferred — парный с 4.7 / 4.8)* `index.html` (~1810 строк) → Vite + Vue 3 SFC. В одной ветке делать нельзя: парная с миграцией Tailwind CDN → Vite (4.7) и Vue CDN → ESM-сборка (4.8), требует отдельного фронт-репо/папки `frontend/` с `npm ci`/`npm run build` в CI и обновления CSP. Оформить отдельной веткой `devin/frontend-bundling` с предварительным `mv index.html frontend/legacy.html` коммитом для удобства review.
- [ ] **5.10** *(deferred — собирается в кучу с 5.4)* Несогласованный стиль логов (русский/английский, emoji/нет, `[*]`/`[?]`/`[!]`/`✓`/`!!!`). Лучше делать одной веткой после 5.4: при переходе на `logging` всё равно придётся писать новые message-шаблоны, и тогда же сразу выровнять формат единым стилем (предложение: только английский, без emoji, формат `<level> <component> <event>: <details>`). Делать в этой же ветке нельзя — изменит каждый файл-сценарий, который ещё не покрыт сценарными тестами.
- [x] **5.11** Из источника вынесены тунеблы в `config.json`: блок `cleanup` (`name_similarity_strict`, `name_similarity_fuzzy`, `name_group_warn`, `name_group_max`) — раньше были модульные константы в `cleanup_duplicates.py`; блок `rezka` дополнен `score_min_with_ids`/`score_min_without_ids`/`score_min_resync_with_ids` — раньше `min_score = -150 if has_ids else 70` и `min_score = -50 if has_ids else 70` хард-кодом в `rezka_sync.py`. Defaults байт-в-байт совпадают с историческими. Операторы могут перетюнить без правки кода. Файлы: `config.json`, `cleanup_duplicates.py`, `rezka_sync.py`.

## 6. Надёжность / отказоустойчивость

- [x] **6.1** `@app.on_event("startup"/"shutdown")` заменены на единый `@asynccontextmanager lifespan` (`main.py`). FastAPI 0.93+ deprecated, на 0.136 ещё работает с warning, но в будущем снимут. Теперь startup-фаза (task_queue.start, FTS init, WAL boot-checkpoint, периодический checkpoint task, rezka session init) и shutdown-фаза (stop-flаги, graceful proc termination, task_queue.stop) — в одном context manager. Файл: `main.py`.
- [x] **6.2** Все `asyncio.get_event_loop()` заменены на современные эквиваленты: внутри async функций — `asyncio.get_running_loop()` (`main.py:218,538,1336`); для замера времени — `time.monotonic()` вместо `loop.time()` (`main.py:858`). `get_event_loop()` deprecated с 3.10 и на 3.12+ может создавать новый loop. Файл: `main.py`.
- [x] **6.3** Добавлен глобальный `_main_loop`, захватываемый в lifespan-startup, и хелпер `_broadcast_threadsafe(message)`, который через `asyncio.run_coroutine_threadsafe` шлёт WS-broadcast на main loop из любого worker-thread. `_sync_rezka_folder_wrapper` крутится в `run_in_executor`, раньше дёргал `asyncio.get_event_loop().create_task(...)` — это (а) аттачилось не к тому loop в worker thread, (б) корутины теряли ссылку и не запускались. Теперь идёт через хелпер. Файл: `main.py`.
- [x] **6.4** `ConnectionManager.broadcast` переписан с последовательного `for ws in self.active: await ws.send_json(...)` на параллельный `asyncio.gather(..., return_exceptions=True)`. Раньше один медленный/полу-отвалившийся клиент блокировал всех остальных за собой (TCP-send в half-closed сокет может висеть минутами). Теперь latency = max(slowest), а не sum(all). Упавшие клиенты по-прежнему вычищаются через `disconnect()`. Файл: `main.py`.
- [x] **6.5** Добавлен `GET /health` (вне auth_middleware): SELECT 1 + PRAGMA user_version + размер очереди + статус worker. 200 при ОК, 503 при ошибке БД. `Dockerfile` HEALTHCHECK и `docker-compose.yml` healthcheck переключены на `/health` (раньше указывали на несуществующий `/api/check_auth` — заглушка из 5.8). Тесты: `tests/test_health.py` (2 кейса). Файлы: `main.py`, `Dockerfile`, `docker-compose.yml`.
- [x] **6.6** Добавлен online-backup БД через SQLite Connection.backup API. Три части: (а) `Database.backup_to(dest_path)` — копия через временный файл + `os.replace`, безопасно работает с WAL и не рвёт страницы; (б) CLI `backup_db.py` для cron — поддерживает `--out`, `--out-dir`, `--pages N`, `--rotate N` (хранить максимум N свежих бэкапов); (в) `GET /api/backup/download` — auth-protected, генерит свежий снапшот и отдаёт как файл. Тесты: `tests/test_backup.py` (5 кейсов: создание, сохранность данных, перезапись, авто-создание родителя, отсутствие temp-файлов при failure). Файлы: `db.py`, `backup_db.py`, `main.py`, `tests/test_backup.py`.
- [x] **6.7** Rezka-сессия теперь детектит logout: добавлены `_rezka_session_dead(resp)` (sniffer status 401/403, redirect на /login или /auth, login-popup HTML маркеры в теле) и `_rezka_request(method, url, **kw)` (обёртка вокруг `requests.request`, при dead-response один раз вызывает `_init_rezka_session()` и ретраит с новыми cookies). `_refresh_rezka_folders_cache` и `_sync_rezka_folder` переключены на этот хелпер — раньше logout не вызывал исключения, и Rezka-фолдер просто терял favorite. Тесты: `tests/test_rezka_relogin.py` (12 кейсов: все sniff-пути и ретрай-петля, включая failure re-login). Файл: `main.py`, `tests/test_rezka_relogin.py`.

## 7. Frontend (`index.html`)

- [x] **7.1** PWA cache-busting. `sw.js` шаблон с `__SW_VERSION__` плейсхолдером, который сервер подставляет на лету (sha-1 от mtime+size `index.html`+`sw.js`+`manifest.json`). Стратегии: `network-first` для navigation/HTML, `cache-first` для `manifest.json`/`icon.png`, всё остальное — passthrough. На `activate` вычищаются старые именованные кеши. Файлы: `sw.js`, `main.py`.
- [x] **7.2** (часть 1.6 — сделано) — `apiFetch` для `startSync`/`startFix`.
- [x] **7.3** Документировано в `FRONTEND.md`: `sessionStorage` — auth-токен и view-level фильтры (закрытие вкладки = выход), `localStorage` — пользовательские предпочтения (сохранять между вкладками). Файл: `FRONTEND.md`.
- [x] **7.4** SRI sha384 для Vue 3.4.27 + SortableJS 1.15.0 (`crossorigin="anonymous"`). Tailwind Play подменяется в рантайме и SRI не поддерживает — задокументировано в `FRONTEND.md` с командой `openssl dgst -sha384 -binary | openssl base64 -A` для пересчёта при смене версий. Файлы: `index.html`, `FRONTEND.md`.
- [x] **7.5** Политика зафиксирована в `FRONTEND.md`: `v-html` запрещён, любое будущее использование требует `DOMPurify` или явный sanitizer. Файл: `FRONTEND.md`.
- [x] **7.6** UX-харднинг: 9 `alert()` заменены на `this.showToast()`, добавлен confirm-диалог для `clear_log`, в шапке logPanel показывается duration текущего процесса (`processStartTimes[key]` → 1-секундный `nowTick` ticker → computed `currentProcessElapsedText` форматирует в `45с` / `2м 15с` / `1ч 30м`). Файл: `index.html`.
- [x] **7.7** `tests/test_search_names.py` — 5 кейсов: гомоглифы x↔х, alias-хиты через `item_search_names`, удаление. `normalize_title` корректно сворачивает кириллицу/латиницу. Файл: `tests/test_search_names.py`.

## 8. Идеи новых фич

- [ ] **8.1** Smart-collections (сохранённые фильтры).
- [x] **8.2** Импорт/экспорт коллекций (JSON/CSV). Backend: `Database.export_collections()` / `import_collections(payload, *, replace=False)`; элементы идут по внешней идентичности (kp_id → imdb_id → rezka_url → title+year), а не по autoincrement id, так что экспорт переносится между БД с разными id. `_resolve_item_id(c, ref)` фиксирует приоритет резолва. Эндпоинты: `GET /api/collections/export?fmt=json|csv`, `POST /api/collections/import` (JSON-конверт `{collections, replace}`), `POST /api/collections/import_csv` (raw CSV-тело). `replace=true` затирает membership коллекции перед импортом, default — merge через `INSERT OR IGNORE` (идемпотент). Frontend: три кнопки в шапке «ЗАКЛАДКИ» — `JSON`/`CSV` (download через Blob+temp `<a>`) и `⬆` (file-picker, принимает `.json`/`.csv`). Confirm-диалог даёт выбрать merge vs replace, после импорта показывает report-диалог (создано/обновлено/добавлено/missing). Тесты: `tests/test_collections_export_import.py` (7 кейсов на db-уровне) + `tests/test_api_collections_io.py` (4 кейса на endpoint-уровне). Файлы: `db.py`, `main.py`, `index.html`.
- [ ] **8.3** Уведомления (Telegram-бот, web-push, email-fallback).
- [ ] **8.4** Recommendation engine на основе `user_ratings`.
- [x] **8.5** Кастомные правила фильтрации (regex). Миграция `0002_filter_rules_audit_log.sql` создаёт таблицу `filter_rules (id, name, field, pattern, action, enabled, created_at, updated_at)`. На каждое подключение SQLite регистрируется `REGEXP` UDF (`_sqlite_regexp` через `_compile_filter_pattern` lru_cache). DAO: `list_filter_rules / create_filter_rule / update_filter_rule / delete_filter_rule` с whitelist полей (`title`, `original_title`, `description`) и действий (`hide`, `highlight`) + валидация regex через `re.compile`. В `get_feed` правила `hide` разворачиваются в `NOT (COALESCE(items.{col}, '') REGEXP ?)` — пагинация остаётся корректной; правила `highlight` применяются после fetch и пишут `matched_rules: [name, ...]` в каждый матчнувшийся item. Endpoints: `GET/POST/PUT/DELETE /api/filter_rules`. UI: модал `🏷 ПРАВИЛА` в нижней панели — список с toggle/delete + форма создания (имя / поле / regex / действие). На карточках совпавшие правила показываются жёлтыми бейджами. Тесты: `tests/test_filter_rules.py` (8) + e2e `tests/test_rebind_audit.py::test_filter_rules_*` (3). Файлы: `db.py`, `main.py`, `index.html`, `migrations/0002_*.sql`.
- [ ] **8.6** Trakt.tv / Letterboxd интеграция.
- [x] **8.7** Просмотр трейлеров (TMDB). Миграция `0003_items_tmdb_id.sql` + `ALTER TABLE items ADD COLUMN tmdb_id` в `init_schema`/`check_and_migrate_schema`. `TMDBClient.get_videos(media_type, tmdb_id)` пробует `ru-RU` → `en-US` → `null`, скоринг приоритезирует YouTube + Trailer + Official. `find_by_imdb_id(imdb_id, return_meta=True)` теперь возвращает `tmdb_id` и `media_type` (movie/tv) — без второго round-trip. Endpoint `GET /api/trailer/{item_id}` резолвит `imdb_id → tmdb_id` (если ещё не закешировано), кеширует в `items.tmdb_id`, возвращает `{youtube_key, name, type, official, tmdb_id, media_type}`. UI: кнопка `🎬 ТРЕЙЛЕР` на карточке открывает модал с YouTube iframe (через `youtube-nocookie.com/embed/{key}?autoplay=1`). CSP уже разрешает `frame-src https://www.youtube-nocookie.com`. Файлы: `db.py`, `main.py`, `tmdb_client.py`, `index.html`, `migrations/0003_*.sql`.
- [x] **8.8** Release timeline в карточке. Данные уже batch-загружены через `db.get_feed` (см. 3.6) — раньше использовался только `item.releases[0].link` (кнопка RUTOR), остальные релизы пропадали. Добавлена раскладка таймлайна в карточке между source-icons и кнопкой СМОТРЕТЬ: компактная строка `📅 Релизов: N — DD.MM.YYYY` (последняя дата), при клике раскрывается вертикальный таймлайн (max-h-44, scroll) с одной точкой на релиз — самая свежая красная, остальные серые. Каждая точка показывает дату, качество (4K HDR / 1080p / WEB-DL и т.п.), размер и ссылку на торрент. State expansion ведётся в `expandedReleaseTimelines: {item_id: bool}`, метод `toggleReleaseTimeline(itemId)` флипает ключ; `formatReleaseDate(s)` рендерит `DD.MM.YYYY` (без времени, в отличие от `formatDate`). Backend менять не пришлось — get_feed уже отдаёт `releases` сорт по `date_added DESC` (`db.py`). Файл: `index.html`.
- [ ] **8.9** Подписки на релизы.
- [ ] **8.10** Адаптеры под другие трекеры (rutracker, nnmclub, kinozal).
- [ ] **8.11** Stremio/Jellyfin плейлист.
- [x] **8.12** Встроенный плеер вместо m3u-скачивания. Новый endpoint `GET /api/stream_url/{item_id}` повторяет логику `stream_m3u` но возвращает JSON `{url, quality, title, is_hls}`. UI: кнопка `▶ Встроенный плеер` в стрим-модале — рендерит `<video controls autoplay>` для mp4 или подгружает `hls.js@1.5.15` из cdn.jsdelivr.net (CSP ✓) для m3u8 на десктопных Chrome/FF. Safari/iOS играют HLS нативно (детекция через `canPlayType('application/vnd.apple.mpegurl')`). При закрытии стрим-модала `hls.destroy()` гарантирует отсутствие утечек воркеров. Файлы: `main.py`, `index.html`.
- [ ] **8.13** «Continue watching» — прогресс просмотра.
- [ ] **8.14** Dark mode.
- [ ] **8.15** PWA offline (последний фид + кэшированные постеры).
- [ ] **8.16** Consensus метаданных из 2-3 источников параллельно.
- [x] **8.17** «Привязать вручную» KP/IMDb/Rezka. Endpoint `POST /api/rebind/{item_id}` принимает `{kp_id?, imdb_id?, rezka_url?}` (только переданные поля обновляются), пишет `before` и `after` в `audit_log` (action=`rebind`), сбрасывает флаги `is_metadata_fixed`/`is_reprocessed` и check-маркеры так что fix/reprocess их подхватит. UI: кнопка `✏ ПРИВЯЗАТЬ` в карточке открывает модал с тремя инпутами; submit отправляет только изменённые поля. Тесты: `tests/test_rebind_audit.py` (5 кейсов). Файлы: `main.py`, `index.html`.
- [x] **8.18** Audit log + undo. Таблица `audit_log (id, created_at, action, item_id, field, old_value, new_value, undone)` с индексами по `created_at DESC` и `item_id`. DAO: `append_audit / list_audit / mark_audit_undone`. Endpoints: `GET /api/audit_log?limit=N&item_id=...`, `POST /api/audit_log/{id}/undo` (для action=`rebind` восстанавливает kp_id/imdb_id/rezka_url из `old_value`). UI: модал `↶ АУДИТ` в нижней панели — список последних 100 операций, на каждой кнопка `↶ Отменить`; уже отменённые показываются disabled. Тесты: `tests/test_audit_log.py` (4) + e2e undo `tests/test_rebind_audit.py::test_undo_*` (1). Файлы: `db.py`, `main.py`, `index.html`, `migrations/0002_*.sql`.
- [ ] **8.19** `/metrics` (prometheus) + Grafana.
- [ ] **8.20** Public share-ссылки на коллекции.

---

## Сводка по этой ветке

| Категория | Всего | Сделано | В работе | Осталось |
|-----------|-------|---------|----------|----------|
| 1. Критические | 7 | **7 (все)** | 0 | 0 |
| 2. Безопасность | 5 | **5 (все)** | 0 | 0 |
| 3. Логика | 21 | **21 (все)** | 0 | 0 |
| 4. Перформанс | 9 | **6 + 3 deferred** | 0 | 0 |
| 5. Архитектура | 11 | **8 + 3 deferred** | 0 | 0 |
| 6. Надёжность | 7 | **7 (все)** | 0 | 0 |
| 7. Frontend | 7 | **7 (все)** | 0 | 0 |
| 8. Фичи | 20 | 7 (8.2, 8.5, 8.7, 8.8, 8.12, 8.17, 8.18) | 0 | 13 |

Эта ветка `devin/test` мерджится в `main` только после явного апрува.
