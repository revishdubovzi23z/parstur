# REVIEW.md (архив)

Полный ревизионный отчёт от 2026-05-12 (коммит `602e49b`) исторически
жил здесь. **Все ~30 описанных в нём пунктов закрыты** — CI, dead
code, persistence, `_session_tokens`, login rate-limit, rezka
retry-loop, decomposition `main.py`/`db.py`, миграция на Vite,
SQLite-magic-валидация импорта БД и т.д.

Историю исправлений можно увидеть в `git log` и в закрытых PR
(`#1`..`#24+`). Открытые пункты на будущее перенесены в
[`ROADMAP.md`](ROADMAP.md) (Этапы 10 хвост, 11, 12).

Свежие аудиты — отдельные .md-файлы за пределами репо (присылаются
во вложении к session). Если хочется зафиксировать новый отчёт в
git — клади в `docs/audits/REVIEW-YYYY-MM-DD.md`.
