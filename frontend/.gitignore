# Build output and node_modules are produced by `npm run build` and
# `npm ci`; both are derivative and should never be checked in.
node_modules/
.vite/
*.tsbuildinfo

# Editor / OS noise scoped to the frontend workspace.
.DS_Store
Thumbs.db
