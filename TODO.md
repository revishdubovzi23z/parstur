# Список невыполненных задач (TODO)

## 🟡 Важные улучшения
- [ ] **7. Доработать статистику** — добавить количество дубликатов, процент заполненности метаданных для всей базы, статистику просмотренных элементов и даты последних запусков для каждого источника в дашборд.
- [ ] **9. Обогащение Игр/Софта (Кнопка 1.2)** — реализовать интеграцию с IGDB/RAWG API или парсинг описаний напрямую со страниц Rutor для не-видео категорий.

## 🔧 Технические улучшения
- [ ] **17. Переход на asyncio** — переработать `main.py` для отказа от `subprocess.Popen` в пользу `asyncio.create_subprocess_exec` и внедрить очередь задач для управления фоновыми процессами.
- [ ] **18. Graceful shutdown и чекпоинты** — внедрить обработку сигналов (SIGTERM/SIGINT) в скрипты и механизм промежуточных сохранений (через JSON/DB), чтобы можно было возобновлять процессы с места остановки.
- [ ] **19. Конфигурация лимитов** — вынести жестко прописанные лимиты (глубина парсинга страниц, количество элементов для фикса и др.) в файл `.env` или отдельный JSON-конфиг.
- [ ] **20. HTTP-кэширование** — добавить кэширование ответов внешних API (TMDB, Kinopoisk) для предотвращения лишних запросов и экономии лимитов (например, через `requests-cache`).

## 💡 Новые фичи
- [ ] **21. Уведомления** — реализовать Telegram-бота для отправки отчетов о завершении длинных пайплайнов или браузерные Push-уведомления.
- [ ] **22. Планировщик задач** — добавить возможность настройки расписания (Cron) для автоматического запуска «Полного цикла» или отдельных этапов синхронизации.
- [ ] **23. Экспорт данных** — добавить функционал выгрузки текущей базы (или отфильтрованного списка) в форматы CSV или JSON.
- [ ] **24. Ручной ввод ID** — добавить в UI интерфейс (модальное окно) для ручного указания/редактирования KP ID или IMDb ID для элементов, которые не удалось найти автоматически.
- [ ] **25. Фильтр «Новое с последнего визита»** — реализовать сохранение времени последнего визита пользователя и отображение бейджей «NEW» для элементов, добавленных после этой даты.
